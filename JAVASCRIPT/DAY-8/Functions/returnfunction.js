console.log("return keyword in function");

//! Creating a function which will perform addition
//! of two numbers

function addNum(num1, num2) {
  let result = num1 + num2;
  return result; // return keyword
}

//! Calling the function
addNum(10, 20); // No Output
console.log(addNum(10, 20));
